[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mac-vendors-sdk"
version = "0.1.0"
description = "Async HTTP client SDK for the mac-vendors public REST API"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [
    { name = "LizardSystems", email = "support@lizardsystems.com" }
]
keywords = [
    "mac-address",
    "oui",
    "ieee",
    "vendor",
    "lookup",
    "api",
    "sdk",
    "async",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: System :: Networking",
    "Typing :: Typed",
]
dependencies = [
    "httpx>=0.27",
    "pydantic>=2.5",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3.0",
    "pytest-asyncio>=0.24.0",
    "pytest-httpx>=0.30.0",
    "ruff>=0.8.0",
    "mypy>=1.13.0",
    "pre-commit>=3.5.0",
]

[project.urls]
Homepage = "https://github.com/mac-vendors/mac-vendors-sdk"
Repository = "https://github.com/mac-vendors/mac-vendors-sdk.git"

[tool.hatch.build.targets.wheel]
packages = ["src/mac_vendors_sdk"]

[tool.ruff]
target-version = "py310"
line-length = 100
src = ["src"]

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP", "B", "C4", "RUF"]
ignore = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["mac_vendors_sdk"]

[tool.mypy]
python_version = "3.10"
strict = true
warn_return_any = true
warn_unused_ignores = true
disallow_untyped_defs = true

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
